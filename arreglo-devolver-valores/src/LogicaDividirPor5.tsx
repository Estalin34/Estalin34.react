
// Define la función `dividirPor5` que recibe un arreglo y devuelve un nuevo arreglo con los valores divididos por 5.
export const dividirPor5 = (arreglo: number[]): number[] => {
  return arreglo.map((valor) => valor / 5);
};
